services:
  kounga_erp.api:
    image: ${DOCKER_REGISTRY-}koungaerpapi
    build:
      context: .
      dockerfile: kounga_erp.api/Dockerfile

  database:
    image: postgres

  pgadmin:
     image: dpage/pgadmin4:9.8

volumes:
  postgresdb: